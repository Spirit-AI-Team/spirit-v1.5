"""Categorized model patches for the FSDP2 runtime.

performance.py contains retained throughput optimizations; profiling.py contains
attribution-only ranges; vision.py contains the fixed-grid Vision path; and
adaptation.py contains dtype/DiT compatibility behavior.
"""
